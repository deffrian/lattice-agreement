#include <iostream>

#include "general/network.h"

int main(int argc, char *argv[]) {
//    if (argv[1][0] == 's') {
//        TcpServer server(8090);
//        while (true) {
//            int sock = server.select_client();
//            uint64_t val = read_number(sock);
//            std::cout << val << std::endl;
//        }
//    } else {
//        int sock = open_socket({"127.0.0.1", 0, 8090});
//        while (true) {
//            uint64_t val;
//            std::cin >> val;
//            send_number(sock, val);
//        }
//    }
}
